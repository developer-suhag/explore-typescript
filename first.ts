/* const myNumber = 13;
const myName = "Suhag";
console.log("hello from typescript", myNumber, myName); */

/* let money = 500;
money = "taka poisha hater moila";
money = false; */

let money: number = 500;
let honey: string = "honey is very sweet";
let doney: boolean = false;

const myName: string = "Suhag";

// function add(first: number, second: number): number {
//   const result = first + second;
//   return result;
// }

// const output: number = add(34, 45);

function double(x: number): void {
  console.log(2 * x);
}
double(2);

// multipurpose

/* function add(first: number | string, second: number | string): number | string {
  const result = first + second;
  return result;
}

const output: number = add(34, 45);
const output1: number = add("Suhag", " Mokta");

console.log(output, output1); */
